#you can open files to write to the files. Need to open file first
# need to specify the w argument

"""
with open("haiku.txt", "w") as file:
    file.write("This is the line 1 of the haiku\n")
    file.write("Following the line 2 of the haiku\n")
    file.write("Finishing off with the line 3 of the haiku\n")

if we re-run the same lines with the new strings, they will replace the old lines, overwrite everything
don't have to have file created before hand
"""